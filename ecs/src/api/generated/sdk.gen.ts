// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { DeleteEntityConditionData, DeleteEntityConditionErrors, DeleteEntityConditionResponses, Evaluate2Data, Evaluate2Errors, Evaluate2Responses, EvaluateData, EvaluateErrors, EvaluateResponses, GetEntityConditionData, GetEntityConditionErrors, GetEntityConditionResponses, ListEntityConditionsData, ListEntityConditionsErrors, ListEntityConditionsResponses, UpsertEntityConditionData, UpsertEntityConditionErrors, UpsertEntityConditionResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Evaluate a single item against one entity condition
 *
 * Resolves the condition from the caller's tenant catalog, falling back to the global catalog, then evaluates the item against the OPA sidecar.
 */
export const evaluate = <ThrowOnError extends boolean = false>(options: Options<EvaluateData, ThrowOnError>) => (options.client ?? client).post<EvaluateResponses, EvaluateErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/conditions/{conditionId}/evaluate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Evaluate an item against live CRS project restrictions
 *
 * Fetches restrictions for the given project from CRS using the caller tenant, then evaluates the item against them via the OPA sidecar.
 */
export const evaluate2 = <ThrowOnError extends boolean = false>(options: Options<Evaluate2Data, ThrowOnError>) => (options.client ?? client).post<Evaluate2Responses, Evaluate2Errors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/project-restrictions/evaluate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List entity conditions
 *
 * Returns every global entity condition, which is read-only, plus the conditions the requesting tenant owns. On an id collision the global condition wins. The tenant is resolved from the bearer token and is never accepted as a parameter.
 */
export const listEntityConditions = <ThrowOnError extends boolean = false>(options?: Options<ListEntityConditionsData, ThrowOnError>) => (options?.client ?? client).get<ListEntityConditionsResponses, ListEntityConditionsErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/entity-conditions',
    ...options
});

/**
 * Delete a tenant-scoped entity condition
 *
 * Deletes the requesting tenant's condition for the given id. Global conditions cannot be deleted and are rejected with 409. The tenant is resolved from the bearer token and is never accepted as a parameter.
 */
export const deleteEntityCondition = <ThrowOnError extends boolean = false>(options: Options<DeleteEntityConditionData, ThrowOnError>) => (options.client ?? client).delete<DeleteEntityConditionResponses, DeleteEntityConditionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/entity-conditions/{id}',
    ...options
});

/**
 * Get an entity condition
 *
 * Returns the global condition for the given id when one exists, otherwise the tenant's own. The tenant is resolved from the bearer token and is never accepted as a parameter.
 */
export const getEntityCondition = <ThrowOnError extends boolean = false>(options: Options<GetEntityConditionData, ThrowOnError>) => (options.client ?? client).get<GetEntityConditionResponses, GetEntityConditionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/entity-conditions/{id}',
    ...options
});

/**
 * Create or update a tenant-scoped entity condition
 *
 * Creates or replaces the requesting tenant's condition for the given id. Global condition ids are reserved by the platform and are rejected with 409. The tenant is resolved from the bearer token and is never accepted as a parameter.
 */
export const upsertEntityCondition = <ThrowOnError extends boolean = false>(options: Options<UpsertEntityConditionData, ThrowOnError>) => (options.client ?? client).put<UpsertEntityConditionResponses, UpsertEntityConditionErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/entity-conditions/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
