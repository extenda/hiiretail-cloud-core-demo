// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { CompleteResponseData, CompleteResponseErrors, CompleteResponseResponses, CreateResponseData, CreateResponseErrors, CreateResponseResponses, CreateSurveyData, CreateSurveyErrors, CreateSurveyResponses, DeleteAnswerData, DeleteAnswerErrors, DeleteAnswerResponses, DismissResponseData, DismissResponseErrors, DismissResponseResponses, GetResponseData, GetResponseErrors, GetResponseResponses, GetSurveyData, GetSurveyErrors, GetSurveyResponses, ListResponsesData, ListResponsesErrors, ListResponsesResponses, ListSurveysData, ListSurveysErrors, ListSurveysResponses, UpdateSurveyData, UpdateSurveyErrors, UpdateSurveyResponses, UpsertAnswerData, UpsertAnswerErrors, UpsertAnswerResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * List surveys.
 *
 * Returns a paginated slim list of surveys for the tenant. Runtime callers (POS) receive only RUNNING surveys; admins may filter by status, product, userType, target, or businessUnitId. When businessUnitId is supplied the server runs target-resolution instead of a literal target match.
 */
export const listSurveys = <ThrowOnError extends boolean = false>(options?: Options<ListSurveysData, ThrowOnError>) => (options?.client ?? client).get<ListSurveysResponses, ListSurveysErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/surveys',
    ...options
});

/**
 * Create a survey.
 *
 * Creates a new survey. Structural fields (questions, targets, products, userType) are immutable after this call. Metadata fields (name, description, startsAt, endsAt, disabledManually, sampleRate) may be updated later with PUT. The survey enters runtime as soon as startsAt is reached, disabledManually is false, and endsAt has not passed.
 */
export const createSurvey = <ThrowOnError extends boolean = false>(options: Options<CreateSurveyData, ThrowOnError>) => (options.client ?? client).post<CreateSurveyResponses, CreateSurveyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/surveys',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Fetch a survey definition.
 *
 * Returns the full survey definition for the given surveyId, including the derived status field. Structural fields are frozen from creation; metadata fields may have changed via PUT.
 */
export const getSurvey = <ThrowOnError extends boolean = false>(options: Options<GetSurveyData, ThrowOnError>) => (options.client ?? client).get<GetSurveyResponses, GetSurveyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/surveys/{surveyId}',
    ...options
});

/**
 * Update survey metadata.
 *
 * Updates non-structural fields: name, description, startsAt, endsAt, disabledManually, sampleRate. Structural fields (questions, targets, products, userType) are not accepted and remain immutable from create. At least one property is required. Validates startsAt/endsAt ordering and sampleRate range (0–100).
 */
export const updateSurvey = <ThrowOnError extends boolean = false>(options: Options<UpdateSurveyData, ThrowOnError>) => (options.client ?? client).put<UpdateSurveyResponses, UpdateSurveyErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/surveys/{surveyId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List survey responses (admin).
 *
 * Browse responses for one survey. No PII is returned.
 */
export const listResponses = <ThrowOnError extends boolean = false>(options: Options<ListResponsesData, ThrowOnError>) => (options.client ?? client).get<ListResponsesResponses, ListResponsesErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/surveys/{surveyId}/responses',
    ...options
});

/**
 * Fetch a single response (admin).
 *
 * Returns the response shell with the answers map.
 */
export const getResponse = <ThrowOnError extends boolean = false>(options: Options<GetResponseData, ThrowOnError>) => (options.client ?? client).get<GetResponseResponses, GetResponseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/surveys/{surveyId}/responses/{responseId}',
    ...options
});

/**
 * Dismiss a response.
 *
 * Sets the response status to DISMISSED. The call is upserting: if the response shell does not yet exist, it is created and then transitioned to DISMISSED.
 */
export const dismissResponse = <ThrowOnError extends boolean = false>(options: Options<DismissResponseData, ThrowOnError>) => (options.client ?? client).patch<DismissResponseResponses, DismissResponseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/surveys/{surveyId}/responses/{responseId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create an empty response shell.
 *
 * Explicitly creates the response shell in IN_PROGRESS from the supplied context without writing any answers. Idempotent on responseId.
 */
export const createResponse = <ThrowOnError extends boolean = false>(options: Options<CreateResponseData, ThrowOnError>) => (options.client ?? client).post<CreateResponseResponses, CreateResponseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/surveys/{surveyId}/responses/{responseId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Complete a response.
 *
 * Validates the stored answers against the survey definition and transitions the response to COMPLETED when every mandatory applicable question has been answered.
 */
export const completeResponse = <ThrowOnError extends boolean = false>(options: Options<CompleteResponseData, ThrowOnError>) => (options.client ?? client).post<CompleteResponseResponses, CompleteResponseErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/surveys/{surveyId}/responses/{responseId}/complete',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove one answer.
 *
 * Deletes the (responseId, questionId) answer row and refreshes the response updatedAt. Idempotent — deleting a non-existent row returns 204 No Content.
 */
export const deleteAnswer = <ThrowOnError extends boolean = false>(options: Options<DeleteAnswerData, ThrowOnError>) => (options.client ?? client).delete<DeleteAnswerResponses, DeleteAnswerErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/surveys/{surveyId}/responses/{responseId}/answers/{questionId}',
    ...options
});

/**
 * Upsert one answer.
 *
 * Writes one answer row keyed on (responseId, questionId) and refreshes the response updatedAt. Idempotent — replaying the same body is a no-op. If the parent response shell does not yet exist it is created in IN_PROGRESS from the request context.
 */
export const upsertAnswer = <ThrowOnError extends boolean = false>(options: Options<UpsertAnswerData, ThrowOnError>) => (options.client ?? client).put<UpsertAnswerResponses, UpsertAnswerErrors, ThrowOnError>({
    security: [{ scheme: 'bearer', type: 'http' }],
    url: '/surveys/{surveyId}/responses/{responseId}/answers/{questionId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
